# CareConnect Mobile (Flutter)
TBD — Flutter app will be initialized here.

## Architecture or State Management:

This application uses Riverpod for state management. Riverpod was chosen for its testability, separation of concerns, and scalability. All business logic is implemented in StateNotifiers, while UI components consume state via Consumer widgets.
